SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[voteOption](
	[optionId] [int] IDENTITY(1,1) NOT NULL,
	[optionName] [nvarchar](30) NOT NULL,
	[voteId] [int] NOT NULL
) ON [PRIMARY]
GO
ALTER TABLE [dbo].[voteOption] ADD PRIMARY KEY CLUSTERED 
(
	[optionId] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, SORT_IN_TEMPDB = OFF, IGNORE_DUP_KEY = OFF, ONLINE = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
GO
ALTER TABLE [dbo].[voteOption]  WITH CHECK ADD FOREIGN KEY([voteId])
REFERENCES [dbo].[vote] ([voteId])
GO

