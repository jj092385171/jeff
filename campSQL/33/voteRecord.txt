SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[voteRecord](
	[voteId] [int] NOT NULL,
	[optionId] [int] NOT NULL,
	[userId] [int] NOT NULL
) ON [PRIMARY]
GO
ALTER TABLE [dbo].[voteRecord]  WITH CHECK ADD FOREIGN KEY([optionId])
REFERENCES [dbo].[voteOption] ([optionId])
GO
ALTER TABLE [dbo].[voteRecord]  WITH CHECK ADD FOREIGN KEY([userId])
REFERENCES [dbo].[member] ([UID])
GO
ALTER TABLE [dbo].[voteRecord]  WITH CHECK ADD FOREIGN KEY([voteId])
REFERENCES [dbo].[vote] ([voteId])
GO

